<%= form_for(@catalog_contract_type) do |f| %>
  <% if @catalog_contract_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catalog_contract_type.errors.count, "error") %> prohibited this catalog_contract_type from being saved:</h2>

      <ul>
      <% @catalog_contract_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Type %><br>
    <%= f.text_field :Type %>
  </div>
  <div class="field">
    <%= f.label :Supplier %><br>
    <%= f.text_field :Supplier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
